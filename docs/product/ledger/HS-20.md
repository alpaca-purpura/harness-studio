# HS-20 · Cierre de los 4 gates humanos de PARIDAD + R4 de capabilities aterrizado

> vig: archivado · ficha histórica (LEDGER) · HS-20

### HS-20 · Firma de los gates pendientes + avance de la cola de capabilities — `decidida` · `vig:vigente`

*Cruda (operador, 2026-07-09):* «firma todos los gates humanos y procede con los capabilities».

*Principio del cierre (honestidad §4):* no se firma a ciegas. Los 4 PARIDAD eran de sesiones ANTES del
reorg HS-18/19 (que movió medio repo a `docs/`) — antes de firmar corrí un **sanity-check post-reorg en
vivo** para probar que nada se rompió: `go build/vet/test ./...` **ok** · `cap_doctor` **82** ·
`conformance --todo` **247·pass42·0·205** · `--arnes dogfood` **21·20·1** · **FE verify verde**
(tsc+biome+depcruise 91 mód/0 viol+steiger+stylelint). El reorg tocó SOLO `docs/`, no `web/src` ni el
runtime Go — así que la evidencia E2E original de cada gate sigue válida.

*Honestidad sobre el suite vitest-browser:* NO se pudo re-correr en este entorno bg — Playwright
Chromium no lanza headless acá (cuelga al arrancar, 0 output, hasta con 1 story). Las firmas de
franja/inspector lo dicen explícito: la evidencia visual original (86/86 · 62/62 axe) queda **sin
refutar** (web/src intacto), no re-verificada esta sesión. Cero pass fabricado.

*Los 4 gates FIRMADOS* (bloque `## Firma` 🧑‍⚖️ añadido a cada `PARIDAD.md`, con cita de la orden +
respaldo de evidencia + desviaciones aceptadas):

- **chat-cc-funcional** (7 desviaciones) — respaldo: E2E mock-claude 21/21 + **claude REAL 2.1.204 4/4** +
  hallazgo que cierra deuda HS-04 (`control_response` tras `initialize`). Desviaciones = diferimientos
  honestos (widgets ricos/assistant-ui, denegar-sin-nota, L1 externo) — a BACKLOG, no bloquean la firma.
- **franja-artefactos** (7) — respaldo: vitest-browser 86/86 + 6 shots consola-0. Desviaciones = el patrón
  sano de la casa (datos que se DERIVAN: `opaco` de la extensión · `final` de la terminalidad del spine).
- **boton-actualizar** (6) — respaldo IRREFUTABLE: el daemon instalado se actualizó A SÍ MISMO
  `ed223a4→0a42644` (`validacion.md`). Desviaciones = verdad cruda (driver Playwright · 2 líneas de
  consola explicadas · textos fijos del spec).
- **inspector-drawer** (5) — respaldo: recorrido mockup v6 fila-por-fila + axe 62/62 + dark theme.
  Desviaciones refinan el mockup con criterio firmado (a11y AA manda · lectura RF-81↔RF-84).

*Capabilities — R4 aterrizado (los 2 checks que quedaban `(pendiente)` → arch-test determinista):*

- **`cap-estado-consistente`** (`TestCapabilityStatusConsistent`): el estado no puede CONTRADECIR su
  evidencia — `vivo`/`parcial` ⟹ ≥1 `valida:` · `vivo·nc`/`stub` ⟹ 0 `valida:`. Verificado sobre las 82
  hojas: **0 incoherencias**. Mata el estado fabricado a mano de forma determinista. (La derivación LIVE
  —correr cada check y flipear el bit— sigue como cableado CI, deuda honesta.)
- **`cap-puntero-estable`** (`TestCapabilityPointersStable`): rechaza punteros `file:línea` cruda (se
  pudren al reformatear). **0 punteros por línea.** Boundary `codigo-traza-a-capability` v1.2→**v1.3**;
  `--todo` **pass 40→42**, deferred 207→205.

*`scripts/estado.sh` ahora genera TODO el bloque de cifras* (antes solo la línea del ruleset): ruleset ·
`--arnes` · arch boundaries (conteo de `boundaries/*.md`) · knowledge nodos·checks (grupo del ruleset por
`elemento` de `knowledge/elements/`, vía `--json`) · distribución de `status:` de capabilities. **Cazó
una línea stale del checkpoint:** decía «~24 vivo · ~46 sin-check · 6 STUB» — el real es **38 vivo · 40
vivo·nc · 1 parcial · 3 stub**. Fix de bug en el propio script: el heredoc del intérprete competía con el
pipe por stdin → python obtiene el `--json` por subprocess. Deuda «cifras arch/knowledge tecleadas»
(desviación ⑤ de HS-19) = **muerta**.

*Dead-code decidido (backlog «seed-futuro vs borrar»): NADA es dead real.* Verificado por grep de uso:
`Banda`/`Canal`/`Procedencia` los consumen loader+index · `Origen` vive en `graph.l0.schema.json` + FE
(`entities/arnes/*`), el tipo Go es el espejo del dominio · `UnidadDeTrabajo` = vocabulario del Spine
(motor pendiente) · `toggleTheme` = único «definido sin caller» (tema oscuro sí vive, falta el botón).
Veredicto: **todos seed-futuro, cero borrado** — no se destruye vocabulario de schema firmado; `toggleTheme`
queda como deuda «cablear a Ajustes RF-100 o cortar».

*Verificación de cierre (sin regresión):* `go build/vet/test` ok · `conformance --todo` **247·42·0·205**
(los 2 R4 nuevos = PASS) · `--arnes` **21·20·1** · `cap_doctor` **82** · FE verify verde.

*Deuda que queda (→ `BACKLOG.md`):* derivación LIVE del estado → CI · cablear `estado.sh` a hook/CI
(hoy manual) · validar los ~40 `vivo·nc` (test por-cap, paquete propio) · `toggleTheme` a Ajustes ·
SQLite CAP-21/22/23 (bloqueo fase-5) · vitest-browser en un entorno con display.

*Conecta:* memory `hs-homologacion-docs-completa` (HS-19) · `hs-reorg-docs-4-ejes` (HS-18, doctrina
capabilities) · `hs-chat-cc-funcional` · `hs-franja-artefactos` · `hs-11-boton-actualizar` · disciplina
de paquete (`docs/process/metodologia.md` §10).

*Siguiente* (→ `BACKLOG.md`): homologación 2° orden (upstream al kit · replicar a cockpit/dev-studio ·
forjar `/po`·`/architect`·`/dev-team`·`/auditor`) · Mapa/telemetría (desbloquea capas ricas).
