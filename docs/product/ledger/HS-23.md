# HS-23 · Portafolio Slice 0 «Cimientos» — gate humano FIRMADO

> vig: activo · ficha (LEDGER) · HS-23 · gate 🧑‍⚖️ FIRMADO 2026-07-13 · Slice 0 CERRADO

### HS-23 · Cierre de Slice 0: firma del gate humano — `decidida` · `vig:vigente`

*Contexto:* Slice 0 «Cimientos del Portafolio» (dominio/backend, sin FE) construido por Sonnet 5
(constructor) sobre el plan de Fable 5 (arquitecto), tickets T1-T9, commiteados a `main` uno por
uno con gate local verde antes de cada commit — ver [[hs-portafolio-slice0-construido]]. `paridad.md`
escrita con el goal §0 del plan (8 puntos) verificado punto por punto + evidencia E2E vivo contra
la máquina real.

*Firma (operador, 2026-07-13):* revisó los 8 puntos del goal §0 —

1. 7 deltas de dominio D-DOM-1..7 (S0-D1..D11) ✅
2. `go build/test -race` verde · `golangci-lint` limpio · `go-arch-lint` verde ✅
3. `fitness/...` verde (R1-R4 capabilities + boundary nuevo) ✅
4. `estado.sh --check` verde · conformance sin regresión (pass 42) · dogfood sin fail nuevo ✅
5. `pnpm run verify` (web) verde ✅
6. E2E vivo máquina real: 3 formas de instalación · origen vía eslabón CC con versión real de
   `installed_plugins.json` · deriva evaluada contra checkout local (o `deriva-no-evaluable`) ✅
7. 5 capabilities nuevas + boundary enforced · `paridad.md` escrita ✅
8. Un commit Conventional por ticket en `main`, T1-T9, gate §M verde antes de cada uno ✅

— contra `paridad.md` + el E2E vivo pegado ahí (2 bugs reales cazados y corregidos en el momento,
S0-D14/S0-D15: `installed_plugins.json` anida bajo `plugins`; la deriva no se evaluaba con `home`
vacío). Las **5 desviaciones registradas** (fixture de test cambiada · componente arch-lint
registrado un ticket antes · CAP-15 sin change_log · corrupción de archivo entero no sobrevive a
save · `empresas` legado+nueva conviven sin fecha de deprecación) quedaron **ACEPTADAS sin
objeción** — firma sin ida-vuelta.

*Cierre:* `paridad.md` checkbox de firma marcado · `checkpoint.md` (paquete activo → Slice 0
cerrado, «Retomar aquí» apunta a Slice 1) · `BACKLOG.md` (item 0 → construido y firmado, item 1
«FE Portafolio» desbloqueado — `bloqueo`(0) retirado).

*Siguiente:* **Slice 1 «FE Portafolio»** (`BACKLOG.md` item 1) — 3 superficies (Lista/lente
empresa · Wizard-Proyecto · Drawer READ) + Observar + Desvincular, defaults honestos, fix mockup
G1-G9, porte a Storybook. Por disciplina de paquete de trabajo (§10 METODOLOGIA): arranca
releyendo [`mockups/INDEX.md`](../../../mockups/INDEX.md) (línea base = superset del mockup v2 ya
aprobado, `mockups/arnesia-portafolio.html`) antes de tocar UI — mockup→decisiones→spec→PARIDAD,
con firma 🧑‍⚖️ entre etapas.

*Conecta:* HS-22 (modelo firmado, Programa Portafolio) · [[hs-portafolio-slice0-construido]].

*Cierre deuda GAP-2/S0-D6 «re-key del índice a (home,id,scope)» (2026-07-23):* la deuda
registrada acá (A2 acotada — Slice 1 solo la hizo VISIBLE con `idsColisionados` +
`ColisionConfirmDialog`, S1-D2, nunca la resolvió) quedó CERRADA. `IndexPort.Upsert` gana el
parámetro `clave` explícito, requerido — el índice del Mapa (`internal/adapters/index/store.go`)
ya NO re-deriva su llave del propio `g.Arnes.ID` del grafo (que dos arneses de homes distintos
podían compartir, pisándose en silencio). Los 3 callers reales pasan su llave autoritativa:
`ObservarEnMapa` (la `clave` calificada del Portafolio) · `loadArnesDir`/`PUT /api/arneses/{id}`
(el `id` que el caller registró) · `NewTurnReindexer` (el `arnesID` del registro de la sesión).
FE: `new-session-picker.tsx#crear` manda `arnes: e.clave` (no el id pelado) — cierra el círculo
Register→loadArnesDir→Upsert→Session.Arnes bajo la MISMA llave, así que las sesiones nuevas y
sus fetches del Mapa (`client.ts`) quedan consistentes de punta a punta sin ningún endpoint ni
ruta HTTP nuevos (`{id}` sigue siendo un string opaco en las 8 rutas `/api/harnesses/{id}/...` —
cero migración de datos persistidos: `ArnesRegistry`/`Session.Arnes` en disco son
`map[string]string`/campo `string` que no validan formato, las sesiones viejas con id pelado
siguen resolviendo exactamente igual). El `ColisionConfirmDialog` (S1-D2/S1-D20) se retiró de
`portafolio-view.tsx`: ya no protegía nada real. Tests nuevos:
`TestUpsertDosArnesesMismoIDNoColisionan` (la prueba central, `internal/adapters/index`) +
`TestUpsertIndexaBajoLaClaveNoElArnesID` + `TestUpsertClaveVaciaError` +
`TestUpsertSinManifiestoError`; los 4 fakes de `IndexPort` en tests (http/portafolio_test.go ·
usecase/portafolio_test.go · usecase/session_reindex_test.go · fitness/arch_test.go) actualizados
para capturar la clave y asertarla. `go build/vet/test -race` + `pnpm tsc`+lint+vitest+stories
verdes (4 fallas pre-existentes en `new-session-picker.stories.tsx` por un bug de contraste a11y
en `.text-warn`, sin relación, registradas aparte en BACKLOG).
