# HS-26 · Chat dock legible y ergonómico — paquete completo firmado (6 quejas + cariño)

> vig: activo · ficha (LEDGER) · HS-26 · gate 🧑‍⚖️ FIRMADO 2026-07-22

### HS-26 · Paquete `chat-dock-ux` cerrado E2E en el día — `decidida` · `vig:vigente`

*Cruda (operador, 2026-07-22, tres firmas en la misma sesión):* «firmo, prosigue» (decisiones) →
«firmo, implementa» (Gate 1 mockup) → «DE momento todo bien, si encuentro un error lanzaré un
bugfix, firmo» (PARIDAD; modalidad: errores posteriores = bugfix nuevo, el gate no se reabre).

*Detonante:* 6 quejas directas del operador sobre el espacio de conversación + 1 pedido de
«cariño» al markdown a mitad de build (CH-D4b). Paquete
`docs/product/stories/2026-07-22-chat-dock-ux/` (decisiones CH-D1..D6+D4b · mockup
`arnesia-chat-dock-ux.html` · spec as-built · PARIDAD firmada).

*Entregado (commits `39a31cd` + `a8e0e99`):* composer auto-crece a 3 líneas (bug real del
placeholder cazado por E2E) · dock redimensionable con persistencia · **CAP-99
paquete-cerrado-en-el-gate**: deny por ruta de `~/.arnesia` en `onControlRequest`, antes de
grants, sin tarjeta (aclaración clave del operador: aplica al RUNTIME de la app; en el repo sí
se trabaja el paquete) · **CAP-100 conversacion-legible**: el conductor deja de descartar
thinking/tool_use (`assistantEvents`, `EventActivity`, `RolAct`), burbuja por paso
(`EventMessage` cierra, `msgFlushed` anti-duplicado), markdown con chips-de-archivo que copian,
bloques con rótulo+copiar y sintaxis coloreada por tokens.

*Honestidad del cierre:* el E2E vivo con claude REAL cazó una regresión de wire — `contentText`
se comparte entre parsear frames y MANDAR el turno user; los campos nuevos sin `omitempty`
rompieron la API (400 «Extra inputs are not permitted») y envenenaron un mensaje del historial
CC de vitalia. Fix + test de regresión (`TestUserTurnWireSinCamposExtra`) + JSONL curado a mano
(backup `.bak-chdockux`). Desviaciones aceptadas en PARIDAD: tarjeta sin segundos · detección
CH-D6 por substring (blunt adrede) · paralelismo de tool_use pinta solo el último «en curso» ·
app instalada pendiente de `make installer` (deuda en BACKLOG).

*Capabilities:* +2 (CAP-99 usecases · CAP-100 fe-chat), total 100, cobertura 100 %.
