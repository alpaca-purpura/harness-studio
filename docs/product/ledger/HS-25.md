# HS-25 · Cierre de 4 gates humanos de PARIDAD (rebrand · Slice1-FE · Slice2 sello/Identificar · shell-topbar)

> vig: activo · ficha (LEDGER) · HS-25 · gate 🧑‍⚖️ FIRMADO 2026-07-22

### HS-25 · Firma de los 4 gates PARIDAD pendientes — `decidida` · `vig:vigente`

*Cruda (operador, 2026-07-22):* «es que ya lo vi, firma tu nomas, necesito cerrar eso para, si hay un
error, trabajarlo como bugfix» — en respuesta a que el asistente se negó a auto-firmar sin que el
operador confirmara haber corrido la verificación en vivo (§4 honestidad: la firma "no se simula").

*Principio del cierre:* el asistente NO corrió el click-through — el operador confirma haberlo hecho ya,
fuera de esta sesión. Esta ficha transcribe esa confirmación, no la fabrica. Antes de firmar se verificó
que el código de cada paquete existe realmente en el árbol (no solo en la doc): `git status` limpio,
`git log` confirma cada feature commiteada.

*Los 4 gates FIRMADOS* (bloque `## Firma` 🧑‍⚖️ en cada `paridad.md`/`PARIDAD.md`, con cita de la orden +
respaldo de evidencia + desviaciones aceptadas):

- **rebrand-prenter-design-system** (2026-07-15) — tokens PRENTER dark-first aplicados a `web/`;
  `pnpm run verify` verde · `vitest` 127/127+21/21 · Storybook `:6006` screenshot real. Deuda que NO
  bloquea: `mockups/arnesia-shell-A-galaxia.html` (🔒 firmado) sin re-derivar a los tokens nuevos +
  botones/CTA por-módulo siguen en `font.sans` (D8) — ambas pasan a `BACKLOG.md` como ticket propio de
  diseño, no de paperwork. `paridad.md` no existía, se escribió en este cierre.
- **portafolio-slice1-fe** (2026-07-13/14) — 3 superficies del Portafolio cableadas a HTTP real, 34
  stories + E2E vivo (GAP-1 cerrado). 8 desviaciones S1-D16..D22 + golangci-lint preexistente,
  aceptadas. T8 y el amendment S1-D23..D26 ya estaban commiteados (`b35ff6c`, `4a35479`).
- **portafolio-slice2-sello-identificar** (2026-07-17) — huella de path (D29) · modo degradado del Mapa
  (D27, mata el 400) · acción «Identificar» in-situ + re-key (D28). E2E real crudo→degradado→
  identificar→sellado verde. **Corrección de estado:** la nota "sin commitear" del paquete quedó stale
  — verificado con `git log` que el código está en `main` desde `824acd8`. `paridad.md` no existía, se
  escribió en este cierre.
- **shell-topbar-selector-arnes** (2026-07-20) — topbar sin empresa + picker de sesión inline sobre
  Portafolio real. 168/168 verde, R1/R2/R4 cobertura 100%. Deuda fuera de alcance (TS-D16, ya
  documentada): `workspace-stage.tsx:235` deja un `·` colgando sin `puesto`.

*Cierre deuda S1-D16 (2026-07-23):* `capMetas` (`docs/architecture/fitness/capability_trace_test.go`)
tomaba como status root CUALQUIER línea `status:` sin mirar columna — un `scenarios[].status`
(enum `live`/`wip`/`deprecated`) anidado habría pisado el `status:` real de la capability (enum
`vivo`/`vivo·nc`/`parcial`/`stub`), rompiendo R4 en silencio apenas alguien poblara `scenarios:`.
Bug latente confirmado (reproducido con fixture antes del fix, `-run TestParseCapMeta` falló con
`got "live", want "vivo"`). Fix: `status:` solo cuenta a columna 0 (`strings.HasPrefix(ln, ...)`
en vez de `trimmed`), lógica extraída a `parseCapMeta` testeable + regresión
`TestParseCapMetaIgnoraStatusAnidadoDeEscenario`. No requirió `yaml` real ni paquete propio —
fix mecánico de 2 líneas + test. `go build ./... && go test ./docs/architecture/fitness/...` verde.

*Cierre deuda S1-D8 lentes proyecto/marketplace (2026-07-23):* la toolbar del Portafolio tenía
2 lentes disabled+tooltip «próximo» sin slice asignado. Construidas con el MISMO patrón N:M ya
sancionado por `agruparPorEmpresa` (cero cambio de backend, los datos ya viajan en el wire):
`agruparPorProyecto` (vía `instalaciones[].proyecto_path`, «sin proyecto instalado» al final) y
`agruparPorMarketplace` (vía `registriesDe`, «origen desconocido» al final) en
`entities/portafolio/model/selectors.ts`; `ListaAgrupada` despacha por lente
(`agrupadorDe`); botones habilitados en `portafolio-list.tsx`. 7 tests unit nuevos (43/43 verde,
`--project=unit`) + 2 stories `play()` nuevas (`LenteProyecto`/`LenteMarketplace`, 9/9 verde en
`portafolio-list.stories.tsx`, `--project=storybook`). Capability `fe-portafolio/lista-del-portafolio.yaml`
actualizada (pointers + scenario + change_log). Los FILTROS `estado`/`marketplace` (distinta
afordancia — acotan sin reagrupar) siguen disabled, no se les inventa un slice.

*Capabilities:* sin cambios de status en esta ficha — `fe-portafolio/*` (CAP-88..92) ya estaban `vivo`
desde T8/Slice2; `fe-shell` CAP-72 (`rail-de-sesiones`)/CAP-74 (`topbar-breadcrumb-k-dock`) quedan
`vivo·nc` a propósito (decisión ya tomada en el paquete shell-topbar: punteros/escenarios actualizados,
`status` deliberadamente sin tocar a mano hasta que exista derivación LIVE — deuda ya registrada, no se
reabre acá).

*Deuda nueva que queda (→ `BACKLOG.md`):* re-derivar `arnesia-shell-A-galaxia.html` a los tokens PRENTER
+ re-estampar `mockups/INDEX.md` + decidir alcance de botones/CTA (D8).

*Conecta:* `hs-disciplina-paquete-trabajo` (regla de la firma no-simulada) · `hs-20-gates-firmados-r4`
(mismo patrón de cierre consolidado) · memory de esta sesión (re-key vs forja-ciclo-vivo, próxima).
