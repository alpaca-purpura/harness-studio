# HS-16 · Auditoría colateral franja-artefactos §8 — 4 fixes mecánicos + investigación de raíz de los 3 hallazgos grandes + 8 checks conectados + 5 reconocedores del loader — `decidida` · `vig:vigente`

> vig: archivado · ficha histórica (LEDGER) · HS-16

*Cruda (operador, 2026-07-08):* «atacá la deuda colateral chica» → luego, sobre los 3
hallazgos grandes que quedaron deferred: «lanzá subagentes para investigar la causa raíz de
cada uno y consolida todo en un plan e implementalo, respetando arquitectura as code».

**Tramo 1 — deuda chica (los 7 hallazgos de `viabilidad.md` §8, 3 mecánicos + 1 ya-resuelto):**
1. **Cifra stale en CLAUDE.md** — «27 pass real + 211 `deferred`» no reflejaba el ruleset
   vigente. Medido en vivo (`arnesia conformance --todo --json`, no de memoria): **240 checks
   · 28 pass · 212 deferred**. CLAUDE.md corregido a la cifra medida.
2. **Colisión de elemento «hooks»** (`parser.go:105`, basename-only): `docs/architecture/knowledge/elements/
   hooks.md` (12 checks CC) y `docs/architecture/conventions/hooks.md` (3 checks lefthook) se fundían en
   un elemento de 15. Fix: **rename** `docs/architecture/conventions/hooks.md` → `git-hooks.md` (+
   `regla:` frontmatter + `INDEX.md` + comentario `lefthook.yml`) — colisión de raíz
   eliminada sin tocar el motor. Verificado: `hooks`(12) + `git-hooks`(3) separados.
3. **Drift fixture↔loader del dogfood `dev-full-cycle`** — el JSON embebido (mano-alzada,
   pre-Puente-1) traía `canal:beta` por-nodo que el loader real jamás deriva, y le faltaba
   `procedencia:declarado` que el loader SÍ estampa. Fix: **regenerado
   `dogfood/dev-full-cycle.graph.json` desde el loader real** (`arnesia index -o …`) +
   sincronizado el mirror TS (sumó `nombre`/`descripcion`/`spine.categorias` que faltaban) +
   **`TestLoaderDevFullCycle` extendido** a comparar `Canal`+`Procedencia` (cierra el punto
   ciego que dejó pasar el drift). `conformance --arnes` sigue 20/21 pass.
4. **`artifacts.Status` descartado en silencio** — verificado: YA reparado por F2 de
   franja-artefactos; sin acción.

**Tramo 2 — investigación de raíz (3 subagentes en paralelo) de los 3 hallazgos grandes:**
1. **Campos sin semántica → 100% RESUELTO, hallazgo stale.** `ruta[].a` ya valida contra
   todas las cajas + `humano` (`domain.VerificarRutaExiste`, F1) y `necesita[].requerido` ya
   bloquea el spawn de verdad (`box_conductor.go` + `TestConductorEncadenaPorFilesystem`, F3)
   — ambos posteriores a cuando se escribió el hallazgo (2026-07-07), nunca actualizado.
   Cerrado sin código.
2. **Validación-de-composición-en-0 → parcialmente real, 14 checks (no 15 — `sin-huerfanos`
   estaba mal-clasificado `nl-judge`, no `arch-test`).** 3 grupos: **Grupo B (8, sin
   ambigüedad)** — reusan código YA probado, solo faltaba el `arch_test.go` nombrado:
   `sin-huerfanos`/`dead-end`/`ruta-a-existe`/`refina-coherente` (wrapper
   `TestDogfoodComposicionFabricaConforma`, reusa `ConformanceService` contra el dogfood real,
   mismo patrón que `usecase.TestDogfoodArnesConforms`) + `daemon-servable-headless`
   (`TestDaemonServableHeadless`: build+run real de `arnesia serve` como subproceso, HOME
   descartable, `GET /healthz`) + `mint-env-en-launcher` (`TestMintEnvInLauncher`: grep sobre
   `main.rs`) + `segundo-adaptador-posible` (`TestSegundoAdaptadorPosible`: `minimalAgent`,
   un `ports.AgentPort` de juguete, drivea un turno real) + `resume-auto-sana`
   (`TestResumeAutoSana`: fuerza un `--resume` que muere antes de `init` con fakes, confirma
   el heal). Los 4 boundaries afectados (`contrato-de-caja-es-fitness-function` v1.4 ·
   `core-no-importa-shell` v1.3 · `adaptadores-de-agente-intercambiables` v1.1 ·
   `sesion-viva-consistente` v1.1) retagueados con el nombre de función real. **Grupo A (6,
   bloqueado por feature que no existe — NO tocado):** `index-reconstruible`/
   `sin-migracion-incremental`/`writer-serializado` (esperan el indexer SQLite fase 5) ·
   `hooks-desde-otel`/`ctx-derivado-etiquetado` (esperan OTel, cero instrumentación hoy) ·
   `modo-por-fase` (esperan permission-mode-por-fase, hoy fijo a `default`). **Grupo C (3,
   necesitan decisión de diseño — NO tocado):** `gate-honesto` (severidad error, no existe
   noción codificada de «gate honesto» vs fantasma) · `transport-sin-reglas` ·
   `agencia-dentro-del-frame`.
3. **Mapa-por-defecto excede al loader → corrección de dato + 5 reconocedores mecánicos
   implementados.** El hallazgo asumía que `content-studio-full` (showcase kitchen-sink,
   mano-alzado, sin dir real en disco) era el grafo DEFAULT — falso: el default real
   (`dev-full-cycle`, seedeado en `session_service.go`) YA es 100% producible por el loader;
   `content-studio-full` es solo una opción del picker. El gap real SÍ era cierto: de los 10
   reconocedores de `nomenclatura-arnes.md` §3, el loader solo tenía 3 (`skill`/`rule`/
   `hook` forma-plugin). Sumados los 5 mecánicos (sin decisión de diseño nueva, `soporte.go`
   nuevo + `loader.go` extrae `nodosDeHooks` compartido): **`command`**/**`output-style`**
   (archivos sueltos `<id>.md`, frontmatter OPCIONAL a diferencia de skill) · **`mcp`**
   (`.mcp.json`, un server = un nodo, vive en la RAÍZ del arnés en AMBAS formas — no bajo
   `.claude/`) · **`settings`**/**`statusline`**/**`hook` forma-instalada** (las tres celdas
   viven en el MISMO `settings.json`: su presencia = nodo `settings`, la clave `statusLine`
   suma `statusline`, la clave `hooks` reusa `nodosDeHooks`). `nomenclatura-arnes.md` v1.2
   documenta el estado real de implementación. **Pendientes (necesitan diseño, no mecánicos):**
   `subagent` (falta decidir cómo se vincula a la caja que lo invoca) · `plugin` como nodo
   raíz (posible colisión con el manifiesto `arnes.l0.json`) · edges de
   `libreria:`/`maquinaria:`/`terceros:`/`marcas-dormidas:` (el `EdgeTipo` semántico de cada
   uno es diseño, no mecánica).

**Verificación real, no simulada (ambos tramos):** `go build/vet/test ./... -race` verde ·
`golangci-lint` 0 issues (repo propio; 2 issues preexistentes en `provisioner_test.go` son de
la sesión HS-17 concurrente, no tocados) · `pnpm run verify` + `vitest` 90/90 verdes ·
`arnesia conformance --todo` 240/28/212 → tras Tramo 2, los 4 checks de Grupo B pasan de
`deferred` a `pass` real · `--arnes` sobre dogfood sigue 20/21 (mismo diente honesto, sin
regresión) · `TestDaemonServableHeadless`/`TestResumeAutoSana` verificados también bajo
`-race` ×10 (sin flakiness).

**Nota de higiene de repo:** ejecutado con otra sesión trabajando en paralelo el mismo
working tree (HS-17, aislamiento MCP) — confirmado con el operador antes de seguir; cero
archivos de HS-17 tocados, commits por archivo específico, nunca `-A`/`.`.

*Deuda restante (deliberadamente NO tocada — rediseño de motor, no mecánica):* Grupo A (6
checks bloqueados por SQLite fase 5/OTel/modo-por-fase) · Grupo C (3 checks: `gate-honesto`
necesita definir la semántica de «gate honesto» antes de codear) · `subagent`/`plugin` como
nodo raíz/edges de orígenes-de-librería en el loader.

*Conecta:* HS-13 (franja-artefactos, dueño de `viabilidad.md` §8) · HS-11 (loader real,
Puente 1) · HS-09 (Mapa, `inspector.tsx` pinta `canal`/`procedencia`) · HS-10 (nomenclatura
firmada, §3 de los 10 reconocedores).

*Siguiente:* nada bloqueante.
