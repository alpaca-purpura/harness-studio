# HS-19 · Homologación de metodología (3 repos) — firmada + cierre coherente

> vig: archivado · ficha histórica (LEDGER) · HS-19

### HS-19 · Adopción del método del plugin `harness@prenter-marketplace` + gate en vivo — `decidida` · `vig:vigente`

*Cruda (operador, 2026-07-09):* «revisá bien y cerrá la homologación de forma coherente, no asumas
nada, investiga todo».

*Qué se homologó (ya en el árbol al cerrar, commit `c59b196`):* toda doc no-skill/no-CLAUDE vive en
`docs/{product,architecture,process}`; el seam TECH/SISTEMA = `project.config.yaml` leído por
`scripts/harness_config.py` (`--doctor` exit 0, 12 slots); front-door = skill `/pm` seam-driven
(bootstrap closure-gate + 10 estados macro + auto-chain); `CAPABILITIES.md` monolítico → **82 hojas
YAML por-cap** (`docs/product/capabilities/{module}/{slug}.yaml`) + `INDEX.md` generado + enforcer
R1/R2 flipeado a leer el árbol YAML (`capability_trace_test.go`, Task 8); `arch/`+`knowledge/`+
`STACK.md` + los 3 raíz (`VISION`/`METODOLOGIA`/`UX`) → `docs/`. Raíz queda con solo `CLAUDE.md` +
`README.md` + `docs/`.

*El gate NO fue un sello (honestidad §4).* Re-corrí TODO contra el binario/repo real, sin confiar en
lo que decía el checkpoint:

- `harness_config.py --doctor` → **exit 0** · `cap_doctor.py` → **82 caps válidas**.
- `go build/vet/test ./...` → **11 paquetes ok / 0 FAIL**.
- `conformance --todo` → **247 · pass 40 · fail 0 · error 0 · deferred 207** (verificado 2 vías:
  línea de texto + conteo desde `--json`).
- `conformance --arnes dogfood/dev-full-cycle.graph.json` → **21 · pass 20 · fail 1** (el warn honesto
  `art-es-path`, el diente no se silencia).
- rutas viejas (`arch`, `CAPABILITIES.md`, `historias`, `VISION/METODOLOGIA/UX/ESTADO/BACKLOG/LEDGER/STACK.md`)
  **TODAS ausentes en raíz**.

*Hallazgo del gate (lo que el checkpoint escondía):* el claim **«0 links vivos rotos»** de Task 9 era
**FALSO**. Un scan en vivo halló **65 links markdown rotos en 13 docs VIVOS** (`docs/process/metodologia.md`
36 — el doc canónico de método, apuntando a `[CAPABILITIES.md]`/`[ESTADO.md]` muertos · `docs/architecture/INDEX.md`
9 · `docs/product/ux.md` 4 · …). **Causa raíz:** la migración reescribió `arch/`→`docs/architecture/` y
`knowledge/`→`docs/architecture/knowledge/` con prefijo `./` **relativo-a-raíz-repo**, no relativo al
archivo movido (desde `docs/process/metodologia.md`, `./docs/architecture/…` resuelve a
`docs/process/docs/architecture/…` = roto) + renames no propagados (`ESTADO.md`→`checkpoint.md` ·
`CAPABILITIES.md`→`capabilities/INDEX.md` · `LEDGER`/`BACKLOG`/`STACK.md` a `docs/product`/`docs/architecture`).

*Fix del cierre (mecánico + determinista):*

- **Fixer determinista** (resuelve cada link roto a su archivo real vía rename-map + relocación de
  prefijos, recomputa la ruta relativa desde el archivo fuente, y **reporta lo que no mapea** — 0
  unresolved) → **65 links corregidos en 13 docs vivos**, code-fences respetados.
- **~11 punteros stale** sincronizados en docs vivos: `README`/`vision`/`stack`/`arch/INDEX`/`LEDGER`
  header/`metodologia` (`ESTADO.md`→`checkpoint.md`, `CAPABILITIES.md`→árbol `capabilities/`,
  `historias/`→`stories/`).
- **Doctrina `codigo-traza-a-capability` v1.1→v1.2:** su prosa aún decía que el SSoT era el monolito
  `CAPABILITIES.md` (la regla R3 viva misinstruía) — sincronizada al árbol `docs/product/capabilities/`;
  **solo prosa: IDs de check y `enforced_by:` intactos** → ruleset `--todo` sin cambio (247·40·0).
- **Historia congelada respetada:** las filas del Log de `LEDGER.md`, las fichas `ledger/HS-NN.md`, y
  los snapshots en `stories/`·`research/` NO se tocan (registros append-only que describen rutas
  verdaderas en su momento) — se revirtió un over-reach del replace que había tocado la fila HS-18.
  **30 links rotos en snapshots históricos se dejan a propósito** (PARIDAD desviación #7).

*Re-verificación post-fix (sin regresión):* `--todo` **247·40·0·207** · `--arnes` **21·20·1** ·
`cap_doctor` 82 · fitness test ok · **0 links vivos rotos**.

*Cierre:* story `docs/product/stories/2026-07-09-homologacion-metodologia/` → `state: done`,
`chris_verify.signoff: true`; `PARIDAD.md` = 14 filas + 7 desviaciones firmadas. Se sigue el patrón
real del repo (las cerradas quedan en `stories/` con `state`, no se archivan — 8 precedentes).

*Desviaciones VIGENTES (heredadas, → paquetes siguientes):* ① `harness_config.py` es COPIA
project-layer del kit (fix plugin-mode va upstream) · ② `estado` de caps migrado del MD, R4 aún
`pendiente` · ③ `research/` con records cross-cutting · ④ arneses `/po`·`/architect`·`/dev-team`·`/auditor`
NO forjados · ⑤ cifras arch/knowledge tecleadas · ⑥ `CLAUDE-nuevo.md` draft muerto · ⑦ 30 links
históricos sin re-apuntar.

*Conecta:* memory `hs-homologacion-docs-completa` · `hs-reorg-docs-4-ejes` (HS-18, el paso previo) ·
disciplina de paquete (`docs/process/metodologia.md` §10) · doctrina `codigo-traza-a-capability`.

*Siguiente* (→ `BACKLOG.md`): upstream del método al kit `harness@prenter-marketplace` (nueva versión)
· replicar `docs/`+seam+`/pm` a **cockpit** y **dev-studio** · forjar los 4 arneses secundarios.
