# HS-27 · Barrido de deuda viva (continuación) — 6 ítems, CI reparado

> vig: activo · ficha (LEDGER) · HS-27 · CERRADA 2026-07-24 (5/6 resueltos, 1 pausado por el operador)

### HS-27 · Continuación del barrido de «Deuda viva» de `BACKLOG.md` — `decidida` · `vig:vigente`

*Contexto:* la sesión anterior (commits `f7a48f6..cca314a`, sin ficha propia en el LEDGER —
gap detectado al abrir esta) cerró 9/15 ítems de deuda viva: re-key del índice del Mapa a
`(home,id,scope)` (cierra GAP-2) · lentes `proyecto`/`marketplace` de la toolbar del Portafolio
(`fe044e0`) · deep-link `arnesia://` · enforcement R1 a nivel símbolo · otros. Esta ficha
continúa con los 6 ítems restantes, mismo criterio: investigar antes de construir, preguntar
cuando el alcance dependa de una decisión del operador, no fabricar pass.

*Ítem 1 — botón «Correr» de una caja (`inspector.tsx`):* investigado a fondo (backend async
completo y probado, CAP-55, cero superficie FE). El operador pidió alcance **paquete completo**
(mockup→spec→PARIDAD, no ticket chico) + mecanismo **SSE `event: run`** + error 409 **inline sin
toast**. Mockup publicado (Artifact) + `decisiones.md` D1-D4. Antes de firmar, el operador pidió
la explicación funcional del botón — se le dio, grounded en `vision.md` A1-A3 (caja = un paso del
proceso, dueña de una transición) + `run_service.go` real (Correr invoca el conductor T3: Claude
Code headless corriendo DE VERDAD contra el repo real del arnés, rol/permisos derivados de la
META — no una simulación). Tras la explicación, **el operador PAUSÓ el ítem** (no lo rechazó, no
pidió cambios) para seguir con el resto del barrido. Paquete queda abierto en
[`stories/2026-07-23-boton-correr-caja/`](../stories/2026-07-23-boton-correr-caja/INDEX.md),
retomable sin perder nada de lo ya construido.

*Ítem 2 — filtros Estado/Marketplace de la toolbar del Portafolio (deuda S1-D8):* investigado
(`portafolio-list.tsx:134-141` — dos botones `disabled title="próximo"`, cero estado, cero
handler). El operador decidió: widget = **chips toggle** (extiende el patrón de disclosure YA
sancionado por `rules-subband.tsx`, aria-expanded sin portal — cero dependencia nueva, evita el
riesgo de portal+test ya documentado en `focus-trap.ts` para Base UI) · cardinalidad = **multi-
select ambos** filtros. Construido: `filtrarPorSalud`/`filtrarPorMarketplace`/
`marketplacesDisponibles` (selectores puros, set vacío = sin filtro) insertados en el pipeline
de `Cuerpo()` ANTES de la lente (buscar → filtro Estado → filtro Marketplace → recién ahí
reagrupa) · `FiltroDisclosure` genérico (Estado = 3 valores fijos vía `SaludPortafolio`,
Marketplace = N valores dinámicos vía `registriesDe`) · `SinResultados` ahora limpia búsqueda Y
ambos filtros con un solo botón. 8 tests unit nuevos + 2 stories `play()` nuevas
(`FiltroEstadoAcota`/`FiltroMarketplaceAcota`) + `ConDatos` extendida con la interacción real del
disclosure. Sin cambio de backend (100% client-side, igual que la búsqueda ya existente). `pnpm
run verify` limpio + `vitest --project=unit` 52/52 + `vitest --project=storybook` 136/140 (los 4
fallos son el bug de contraste `.text-warn` YA documentado y explícitamente excluido de este
barrido, sin relación). Capability `arnesia.fe-portafolio.lista-del-portafolio` (CAP-89)
actualizada — `status: vivo` se mantiene, nuevo `change_log` + escenario
`lista-filtros-estado-marketplace-acotan-sin-reagrupar`.

*Honestidad del cierre (ítem 2):* no se creó un paquete nuevo de trabajo (mockup→spec→PARIDAD) —
mismo criterio que `fe044e0` (lentes proyecto/marketplace): extensión mecánica de un patrón ya
firmado (S1-D8, Slice 1 FE), sin visual nuevo que diseñar y con las decisiones de widget/
cardinalidad ya resueltas por el operador vía pregunta directa antes de codear.

*Ítem 3 — telemetría JSONL → indexer real (+ "telemetria-de-nacimiento", mismo trabajo visto
desde dos ángulos):* investigación inicial separó el ítem en 3 tamaños distintos (Tokens
recortable ya · Desempeño/Proceso genuinamente bloqueadas). El operador, a mitad de la
investigación, recordó un dato clave que el escrutinio inicial no tenía: *"En su momento habías
creado algo en Langfuse pero se dejó de usar"* + *"antes todo estaba aquí:
~/Proyectos/prenter-harness (repo legacy)"*. Ese aviso destapó un hallazgo real: un emisor de
telemetría YA CONSTRUIDO y probado (`prenter-harness/products/kit/core-harness/telemetry/
emit.py`, KIT-03, 508 líneas) que parsea JSONL y exporta OTLP a un Langfuse que sigue corriendo
HOY en la máquina (6 contenedores Docker activos). Antes de portarlo, el operador puso la
restricción rectora: *"arnesia es un producto instalable... no vamos a entregar un manual para
que el usuario instale y conecte langfuse... piensa en cómo debe ser nuestra arquitectura y con
eso tomá una decisión, documentá y ejecutala"*.

Verificado contra la doc oficial de Claude Code (`code.claude.com/docs`, agente
`claude-code-guide`): la telemetría OTel NATIVA (`CLAUDE_CODE_ENABLE_TELEMETRY`) ya emite
`claude_code.token.usage`/`claude_code.cost.usage` con atribución `skill.name`/`tool_name`
limpia — sin hook custom, sin Langfuse, sin tocar el JSONL. **Arquitectura resuelta:** receptor
OTLP embebido loopback-only en el daemon Go + scaffold inyecta env vars (nunca infraestructura
externa) + JSONL sigue SOLO enumerar/replay (coherente con `conductor-no-parsea-jsonl.md`,
severity `high`, que un port literal de `emit.py` habría violado) + Langfuse 100% opcional,
nunca dependencia del producto — el contenedor corriendo hoy es observatorio propio de
alpacapurpura, no infraestructura de ningún arnés instalado. Documentado en
`docs/architecture/boundaries/telemetria-de-nacimiento.md` (v1.0→v2.0, 4 checks) +
`stories/2026-07-24-telemetria-embebida-otel/` (decisiones D1-D5). **Cero código construido a
propósito:** toca UI nueva del Mapa (capa Tokens, sin diseño visual en ningún mockup existente,
ni siquiera el "destino") — es un paquete propio que requiere su mockup, no algo saltable.
BACKLOG separa correctamente: Tokens ya no es `bloqueo` (diseño resuelto, falta construir) ·
Desempeño/Proceso siguen `bloqueo` genuino.

*Ítem 4 — chat "fase presentación" (decisión #5, assistant-ui/CodeMirror/widgets ricos):*
investigado — la nota de 2026-07-08 agrupaba 6 sub-ítems bajo un rótulo; HS-26 (2026-07-22) ya
resolvió 2 a mano sin librerías (markdown vía `react-markdown`, tarjetas de tool-use vía
`ActivityCard`), confirmando sin buscarlo la premisa de la propia decisión #5 ("la librería no
cambia qué funciona, cambia cómo se pinta"). El operador eligió **solo re-scopear, no
construir**: BACKLOG separa 4 sub-ítems reales e independientes (widgets ricos para turnos
`sys` · slash-menu · cola de turnos · diff editable `@codemirror/merge`, este último el único que
genuinamente pediría una librería) y **descarta el swap completo a `@assistant-ui/react`** — no
cierra ninguna brecha funcional ya cerrada a mano, con costo real de bundle (+154kB) sin
beneficio claro hoy. Corregido el comentario stale en `permission-card.tsx:162-163` que apuntaba
a la librería en vez del alcance real.

*Ítem 5 — 212/215 checks `deferred` del ruleset `--todo`:* desglose numérico completo (no al
ojo): 134 son catálogo de doctrina de Claude Code sin target implementado (fuera de alcance real,
ni `--arnes` ni `--todo` lo validan) · 44 son `nl-judge` YA enforced en otro job de CI
(dependency-cruiser/biome/stylelint/tsc/golangci-lint/lefthook/vitest) que el motor Go no sabe
reportar como tal · ~24 son deuda de diseño ya admitida (`gate-honesto`/`agencia-dentro-del-frame`/
HS-16 Grupo A) · 9 genuinamente accionables. De esos 9: **3 arreglados** — bug real en el adapter
`GoArchLint` (`internal/adapters/conformance/mechanism/adapters.go`): probaba
`exec.LookPath("go-arch-lint")`, que nunca está en el PATH (dev y CI invocan `go run pkg@latest`)
— el motor difería localmente algo que CI corre y hace cumplir de verdad. Corregido para invocar
EXACTAMENTE lo mismo que `ci.yml`. Los otros **5 investigados y descartados** como fix chico: el
operador pidió profundizar si valía implementar `SchemaAdapter`/`StaticScan` de verdad (son stubs
que siempre difieren) — sí es posible (`SchemaSet.ValidateJSON` ya existe, reusable), pero 4 de
los 5 checks apuntan al MISMO schema completo, no a un campo específico: un `fail` ahí no podría
distinguir "el campo que a ESTE check le importa está mal" de "cualquier otro campo del schema
falló" — deuda de diseño real (granularidad), no un enforcer de 30 minutos.

*Hallazgo colateral (no relacionado, encontrado investigando el ítem 5):* CI en `main` estaba
roto desde 2026-07-23 por 2 causas nuevas — un `gosec` G703 falso positivo en
`provisioner.go` (`sessionID` es `newID()` interno, nunca input de cliente, mismo patrón que 2
supresiones G304 ya existentes en el mismo archivo) y `internal/adapters/history/**` sin
registrar en `.go-arch-lint.yml` (`cmd` la importa directo, componente nunca declarado — oversight
de cuando se agregó el lector de historial B2). Ambos arreglados. `golangci-lint run` completo
reveló además 10 issues preexistentes más en archivos no relacionados (3 gosec G304 en tests ·
4 govet shadow · 1 nilerr real en `loader/regla.go` [un `os.Stat` que fallaba por motivo distinto
a "no existe" se trataba igual, ahora distingue] · 1 noctx · 1 nolint stale · 2 perfsprint) —
todos arreglados, cero cambio de comportamiento salvo el nilerr (ahora un error real de stat que
no sea "no existe" se propaga en vez de tratarse como "sin rules/, legal"). `golangci-lint run` +
`go-arch-lint check` + `go test ./... -race` limpios de punta a punta — el `text-warn` a11y del
job `ts` queda como la ÚNICA causa conocida de rojo en CI, sin tocar (fuera de alcance,
explícitamente excluido por el operador).

*Ítem 6 — HS-16 Grupo A/C, ¿siguen bloqueados?:* re-verificación corta, confirmado SIN cambios
desde 2026-07-08. Los 6 checks del Grupo A (SQLite fase 5 ×3, `modo-por-fase`,
`hooks-desde-otel`) siguen bloqueados tal cual — ninguno tocado por la resolución de
arquitectura de telemetría del ítem 3 de esta misma ficha (esa cubre solo Tokens;
`hooks-desde-otel` necesita Desempeño, confirmado fuera de alcance). `gate-honesto` (Grupo C)
también sin cambios, deuda de diseño pura. Hallazgo colateral SIN construir (el operador eligió
solo documentar y cerrar el barrido): `ctx-derivado-etiquetado` puede estar stale — HS-16 lo
agrupó con `hooks-desde-otel` como "espera OTel", pero `ctxPct` (`conductor.go:616`, commit
`b91a061`, 2026-07-22, posterior a HS-16) ya lo calcula desde stream-json puro. Registrado en
BACKLOG como candidato al mismo patrón de wrapper-test del Grupo B, sin verificar a fondo.

## Cierre de la ficha (2026-07-24)

6/6 ítems del barrido recorridos: ítem 1 pausado por el operador (paquete abierto, retomable) ·
ítems 2, 4, 5 cerrados con código+docs · ítem 3 resuelto como decisión de arquitectura
documentada (sin código, correctamente — toca UI nueva) · ítem 6 confirmado sin cambios + 1
hallazgo nuevo registrado. `main` queda con CI reparado (salvo el bug a11y ya conocido y
excluido) — commits de esta ficha: filtros Portafolio, arquitectura de telemetría, re-scope
chat, fix CI (gosec+go-arch-lint+10 lint), fix adapter GoArchLint + `TestGoArchLintAdapterCorreDeVerdad`.
