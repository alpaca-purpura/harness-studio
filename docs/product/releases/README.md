# releases/ — hitos (contenedor temporal de stories)

> Un release = `{id}.yaml` (ej `F0.yaml`): `release_id`, nombre, status, `stories[]`, máquina de
> estados propia. Agrupación temporal de stories hacia un entregable. Owner: `/pm`.
> Vocabulario del kit (reemplaza sprint/PI). Mapea a las fases del gran plan de ArnesIA.
