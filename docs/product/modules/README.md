# modules/ — narrativa por módulo de negocio

> Un `{module}.md` por cada `project.config.yaml:domain_modules`. Goal + boundaries + deps +
> tabla de capabilities AUTO-GENERADA (marcador-delimitada, R3) + futuras iteraciones + referencias.
> Owner: `/pm`. La tabla de caps se regenera desde `capabilities/{module}/*.yaml`, no se teclea.
