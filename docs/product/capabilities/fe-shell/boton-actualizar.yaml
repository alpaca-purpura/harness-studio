---
# GENERADO desde CAPABILITIES.md por scripts/capabilities_to_yaml.py — no editar a mano
# (mientras CAPABILITIES.md sea el enforcer). Schema: docs/product/_templates/capability.template.yaml
capability_id: arnesia.fe-shell.boton-actualizar
cap_num: CAP-77
slug: boton-actualizar
name: "Botón actualizar (UI self-update)"
status: vivo          # GENERADO (R4) — no teclear
module: fe-shell
verb: mejorar
pointers:
  - "web/src/features/self-update/ui/update-card.tsx#UpdateCard"
  - "web/src/pages/shell/ui/global-view.tsx#AjustesView"
  - "web/src/shared/lib/platform.ts#isTauri"
valida:
  - "web/src/features/self-update/ui/update-card.stories.tsx"
scenarios:
  - "Estado sinRepo DENTRO de Tauri: la tarjeta gana el botón «Elegir carpeta…» (isTauri() gatea la prop onElegirRepo)"
  - "Elegir carpeta con el diálogo nativo dispara PUT /api/self-update/repo y refresca GET /api/version al confirmar"
  - "Un candidato rechazado por el servidor muestra el motivo exacto inline (repoConfigError), nunca un fallo mudo"
  - "En browser plano (sin Tauri) el botón no existe — el mensaje sigue apuntando a --repo/ARNESIA_REPO"
  - "RF-231: la tarjeta encabeza con el identificador COMPLETO del build (arnesia v0.2.21.2607260225) y muestra cuándo se compiló; el commit queda debajo, porque es otro dato y no responde «¿corro lo último que compilé?»"
  - "RF-231: si hay un build más nuevo en disco, el aviso va en la SUPERFICIE (caja warn), no en un title — un número que hay que comparar de memoria contra el último build no sirve"
  - "RF-231: un build sin sellar lo dice («build sin sellar (dev)») en vez de inventar un número"
business_rules:
  - "RF-110 (bugfix fix-repo-self-update, 2026-07-13): selector NATIVO de carpeta, no input de texto — gesto real de OS, no scriptable por contenido web"
  - "RF-231 (2026-07-26): el aviso de build viejo NO puede aparecer cuando el binario que corre es el último — un aviso que aparece siempre no se lee nunca. De ahí el margen entre el sello y el mtime del propio archivo."
related_capabilities:
  depends_on: []
  enables: []
source_ref: "docs/product/ledger/HS-18.md (barrido 7-subagentes, CAP-77)"
---

# Botón actualizar (UI self-update)

Capability derivada del código real (barrido 7-subagentes, CAPABILITIES.md HS-18).
