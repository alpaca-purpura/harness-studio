# Decisiones del inspector/drawer — log por iteración

> Una entrada por decisión conversada sobre el mockup. Estado: PROPUESTA → FIRMADA.
> Regla: ninguna decisión entra al spec sin firma; ninguna llega al código sin spec.

| # | Fecha | Decisión | Porqué | Estado |
|---|-------|----------|--------|--------|
| 1 | 2026-07-07 | **Botón «ampliar» en el header del drawer**, al costado del de cerrar (⤢). Al click, el drawer ocupa TODO el espacio visual del mapa; mismo botón (⤡) o `Esc` restauran. En modo amplio el contenido reparte las secciones en columnas para aprovechar el ancho. | Operador: «visualizar con más detalles» — 340px queda corto para contratos ricos. | **FIRMADA** (operador, 2026-07-07 · v2) |
| 2 | 2026-07-07 | **Tooltips en dos niveles**: (a) cada título de sección (CLASIFICACIÓN, FUENTE, …) lleva una «i» al costado con tooltip que explica qué agrupa; (b) cada nombre de campo (banda, fase, canal, procedencia, …) se subraya PUNTEADO (affordance de hover) y su tooltip explica qué significa el campo Y qué significa el valor concreto que muestra. Aplica a todos los elementos. | Operador: el drawer debe autoexplicarse — doctrina legible sin salir del nodo (guía sin bloqueo). **Restricción del operador (mismo turno): el contenido de CADA tooltip debe ser consistente con nuestra doctrina** — fuentes: METODOLOGIA §3 (4 ejes del contrato) · §4 (honestidad, gris≠verde) · §8.1/§8.2 (arquetipo/perfil) · VISION A1–A7 · schema L0 (banda/canal/origen) · rules L2.6 (carga). El diccionario vive en el mockup (`SEC_TIP`/`DEF_CAMPO`/`DEF_VALOR`) y al implementar se cementa en la entity FE con las mismas fuentes. | **FIRMADA** (operador «Firmo de momento», 2026-07-07 — redacciones ajustables en paridad) |
| 3 | 2026-07-07 | **Bloque «traer YA» del análisis v3 (`analisis-drawer-v3.md §A`), completo:** (a) sección **VIENE DE** — edges inversos con su tipo (invoca/lee/escribe), derivados del grafo; (b) **chips tipados y NAVEGABLES** en NECESITA/RUTA (origen `caja:`/`base:`/`usuario` tipado; click → selecciona ese nodo) + condición de ruta como **badge** diferenciado; (c) sección **HALLAZGOS** por nodo, versión conformance sin telemetría: `gate:none` (A4) · `no-reconocido` (D-c) · checks rojos del endpoint de conformance filtrados por nodo; «Sin hallazgos abiertos» como estado; (d) **botonera staged al pie** — «Editar conversando» primaria (backend Fase E vivo, se cablea en Fase 3/4) + Evaluar A/B · Promover a estable · Ver en Historia DISABLED y rotuladas, jamás fingiendo funcionar; (e) **estado vacío** del inspector con línea de affordance. | Operador: «Apruebo todo lo que traerías YA» — todo soportado por dato existente (edges del grafo · contrato · conformance endpoint HS-11). | **FIRMADA** (operador, 2026-07-07 · v4 la materializa) |
| 4 | 2026-07-07 | **Tabs `Resumen \| Contenido \| Corridas` en el drawer** (propuesta de `analisis-drawer-v3.md` §tabs, por etapas honestas): **Contenido** = fuente del componente READ-ONLY (viewer con números de línea, chip «versiona con el arnés»; endpoint de lectura confinada `GET …/nodes/{id}/fuente` al implementar; universal por clase — no-reconocido→ver el artefacto que el loader no entendió); acciones «Editar fuente»/«Editar conversando» STAGED (Fase 3/4, backend E vivo). **Corridas** = «corridas donde actuó» con estado honesto «necesita indexer JSONL»; al implementar listan primero las corridas de caja (D2) y la sesión viva; el DETALLE (Conversación/Árbol/Waterfall + replay en el mapa) = vista propia fuera del drawer (Hito 3). En el mockup la fuente se RECONSTRUYE del dato real del grafo, rotulada (el showcase no vive en disco); lo no reconstruible muestra su estado, jamás contenido inventado. | Operador: «Firmo la #4» — trae la autodocumentación (fuente visible) y la cara honesta de la telemetría pendiente. | **FIRMADA** (operador, 2026-07-07 · v5 la materializa) |
| 5 | 2026-07-07 | **UI del modo expandido** (review ojo-UI + reglas del operador): (a) tabs STICKY bajo el header; (b) tabs compactas alineadas a la izquierda en expandido (en drawer normal siguen full-width); (c) botonera del Resumen = fila completa al pie de la rejilla (las acciones son del NODO, no de una sección); (d) columna de lectura max-width ~920px para Contenido/Corridas expandidos; **(e) regla del operador: en expandido, `✕` CIERRA el drawer del todo (queda el mapa sin drawer) y `⤡` colapsa al drawer normal** — cerrar ≠ colapsar. | Review con DevTools destapó tabs desproporcionadas/no-sticky, botonera perdida en la rejilla y Corridas-eco; el operador fijó la semántica de cierre. | **FIRMADA** (operador «Ok» + confirmación explícita «si a toda tu propuesta» + regla ✕/⤡ dictada, 2026-07-07 · v6) |
| 6 | 2026-07-07 | **DEBATE ABIERTO (doctrinal, NO se implementa desde este paquete): ¿la meta del componente migra a un yml hermano?** Planteo del operador: la información de entendimiento (utilidad, cuándo se invoca, casos, si debería existir) puede hacer pesado el componente y gastar contexto en el uso diario → ¿mejor un `.yml` hermano que CC no carga? Mi análisis: (i) tokens/invocaciones/«debería existir» se MIDEN (telemetría), no se escriben; (ii) `description` (CC la usa para decidir invocación) y `why`/`constraints`/`non_goals` (enmarcan al modelo, framed autonomy) son carga ÚTIL — quedan en el archivo; (iii) lo fábrica-only (`gate.aceptacion`/`evidencia`/`ruta`/ejes) es candidato a sidecar `<id>.meta.yml` (CC lo ignora = 0 contexto; el loader lo funde al grafo, nomenclatura v1.1) — PERO partiría el contrato FUSIONADO firmado (METODOLOGIA §3, HS-07/08) = enmienda doctrinal, se decide allá. **Recomendación: spike de MEDICIÓN primero (principio 11, economía de contexto medible)** — sesión real sobre el dogfood, leer su JSONL y medir cuántos tokens del contrato entran al contexto y cuándo; decidir con el dato. | El operador preguntó «¿qué opinas?» — la conversación no es el único registro. | **RESUELTO POR MEDICIÓN** (spike 2026-07-07, → [`spike-medicion-contrato.md`](./spike-medicion-contrato.md)): CC inyecta SOLO el cuerpo del SKILL.md al activar (749 chars ≈ 187 tok); el frontmatter/contrato = **0 tokens** (`perfil_harness`/`escritor_unico` = 0 apariciones en el JSONL). **El yml hermano es innecesario para skills; el contrato fusionado queda validado gratis.** Superficie sensible restante: rules/CLAUDE.md (ya cubierta por identidad mínima). Pendiente menor: subagents/commands presumidos iguales, no medidos. |
| 7 | 2026-07-07 | **El drawer pinta algo o NO existe** (pedido de cambio del operador, post-PARIDAD): al pulsar `✕` el drawer DESAPARECE entero (en drawer normal y en expandido); el estado vacío con la línea «Clic en un nodo del mapa…» MUERE — jamás se muestra una barra vacía. Sin selección el inspector NO se monta (guardia en la página: `selectedBox && <Inspector/>`); `box` pasa a prop requerida; la story `Vacio` muere (71→70) y el CSS `.arnesia-inspector.empty` sale. `expanded` sigue sobreviviendo al cambio de nodo; el reset de tab usa el patrón React «adjust state during render» (biome vetó la dep no usada del efecto). | Operador: «cuando pongo X la barra del drawer debe desaparecer, nunca debe aparecer INSPECTOR/Clic en un nodo…; el drawer pinta algo o no aparece». **SUPERSEDE** el estado vacío de la decisión #3(e) y RF-84 (affordance→ausencia); generaliza la regla ✕ de la #5(e) a todo modo. | **FIRMADA** (operador, 2026-07-07 noche) |
