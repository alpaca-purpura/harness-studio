# Decisiones del botón «Actualizar» — log por iteración

> Una entrada por decisión conversada. Estado: PROPUESTA → FIRMADA.
> Regla: ninguna decisión entra al spec sin firma; ninguna llega al código sin spec.

| # | Fecha | Decisión | Porqué | Estado |
|---|-------|----------|--------|--------|
| 1 | 2026-07-07 | **Alcance = self-update de la app instalada (binario daemon+UI embebida).** El botón reemplaza el binario por la versión nueva y reinicia — lo que hoy el operador hace con `sudo dpkg -i`. (La recarga del arnés ya existe vía `PUT /api/arneses/{id}`; no es este paquete.) | El dolor real es el sudo del ciclo de instalación, no el índice. | **FIRMADA** (operador, 2026-07-07) |
| 2 | 2026-07-07 | **Mecanismo = binario en `~/.local/bin` (espacio de usuario).** El daemon corre como el usuario y puede reemplazarse a sí mismo sin privilegios (write-tmp → rename → re-exec). El .deb queda SOLO para instalación inicial de terceros. Con .deb no existe botón sin sudo: dpkg escribe en /usr (root) — polkit/pkexec solo disfraza el prompt. Estado real hoy: `/usr/bin/arnesia` dueño root (por eso el sudo); migración inicial a ~/.local/bin una sola vez. | Cero privilegios en el ciclo diario; local-first coherente (todo lo del operador vive en su espacio). | **FIRMADA** (operador, 2026-07-07) |
| 3 | 2026-07-07 | **Origen de la versión nueva = build local del repo** (`scripts/bundle.sh` / binario compilado del árbol). Release remoto (GitHub) queda para el release train (KIT-06) — si aparece en la UI antes, va staged rotulado, jamás fingiendo. | Es el ciclo real de dogfood del operador; no hay release train activo que descargar. | **FIRMADA** (operador, 2026-07-07) |
| 5 | 2026-07-07 | **Ubicación del botón = vista global «Ajustes»** (⚙ al pie del rail izquierdo). Hoy esa vista es `ComingSoon` («Ajustes · Marketplaces», `global-view.tsx:14`): la tarjeta «Versión y actualización» la ESTRENA; las demás tarjetas de Ajustes (marketplaces · daemon) quedan punteadas como futuras en el mockup, jamás fingiendo existir. Mockup v2 caso 00 lo muestra con el frame del shell. | Pregunta del operador «¿dónde estará el botón?» — la actualización es configuración del PROPIO daemon/app, no de un arnés ni de una sesión: su casa natural es Ajustes (global), no el Mapa ni el topbar. | **FIRMADA** (operador «firmo el mockup», 2026-07-07 — v2) |
| 4 | 2026-07-07 | **PROPUESTA de seguridad (a cementar en spec):** el endpoint `POST /api/self-update` (a) vive BAJO el confinamiento S1 (Host+Origin+token, superficie-local-confinada); (b) JAMÁS acepta rutas en el request — la ruta del repo la conoce el daemon por configuración explícita (flag/env/registro); (c) el binario recién compilado se verifica (existe, ejecutable, del árbol esperado) antes del rename; (d) todo fallo es visible en la UI (build/instalación/reinicio), nunca silencioso. | Un endpoint que reemplaza el binario y se re-ejecuta ES ejecución de código: misma disciplina que el resto de la superficie local. | **CEMENTADA en spec** (RF-106; + sin params, 409 concurrencia, re-exec post-respuesta) |
| 6 | 2026-07-07 | **FASE 1 (diseño técnico dentro del spec firmado):** `scripts/bundle.sh` gana salida `bin/arnesia` (paso 2, mismo binario que el sidecar) y modo **`--daemon-only`** (SPA + go build, SIN tauri/rustc). El self-update corre `bundle.sh --daemon-only`. | El spec (RF-104 ②) y la validación (install `bin/arnesia`) asumen `bin/arnesia`, que hoy NO existe; y el paso build del mockup dice «SPA + go build» — compilar el instalador Tauri entero (minutos de rust) en cada click sería fingir que hace falta. rustc deja de ser prereq del self-update. | sellada F1 (no altera spec) |
| 7 | 2026-07-07 | **FASE 1: huella = `vcs.revision[:7]`, con sufijo `+sucio` si `vcs.modified`** (buildinfo de Go; binario sin VCS → `dev`). «Ya al día» = igualdad de esa huella compuesta. Limitación honesta: dos builds sucios del MISMO commit comparan iguales (el flujo real commitea antes de actualizar). El JSON de `/api/version` lleva además `sucio: bool` (aditivo a los 5 campos del RF-107). | La revisión sola mentiría sobre un árbol sucio; el buildinfo trae `vcs.modified` gratis. | sellada F1 (aditivo, no altera spec) |
| 8 | 2026-07-07 | **FASE 1: el adapter captura `os.Executable()` AL CONSTRUIRSE** y re-ejecuta ESA ruta. Tras el rename, `/proc/self/exe` en Linux apunta al inode viejo y reporta `… (deleted)` — leerlo tarde re-ejecutaría una ruta que ya no existe. | Detalle de plataforma que rompería el paso ⑤ silenciosamente. | sellada F1 |
| 9 | 2026-07-07 | **FASE 1: tras `resultado=actualizado` el lock del usecase NO se libera** (el proceso es terminal: re-exec en ≤1s; un segundo POST en esa ventana recibe 409 honesto, no un rebuild fantasma). Se libera en `fallo` y `ya-al-día`. FE: el polling del reinicio arranca a los ~1.5s y consulta solo `GET /api/version` (liveness + huella en una llamada; pollear también /healthz duplicaría requests y ruido de consola mientras el daemon está caído). | 409 coherente con RF-106; consola limpia del gate de paridad. | sellada F1 |
