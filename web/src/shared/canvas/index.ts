// shared/canvas — dumb, domain-free canvas primitives. Only this layer + widgets/map-canvas
// may live on the canvas side of the canvas⊥chrome boundary (arch/boundaries/fe-taxonomia-componentes).
export { Glyph, type GlyphShape } from "./glyph"
