{
	"//": "TS estricto (enforced_by de arch/conventions/ts-types.md). Extiende @tsconfig/strictest + overrides Vite. `tsc --noEmit` = el gate. DECLARADO; se activa con la SPA web/ (fase 5).",
	"extends": "@tsconfig/strictest/tsconfig.json",
	"compilerOptions": {
		"target": "ES2022",
		"lib": ["ES2023", "DOM", "DOM.Iterable"],
		"module": "ESNext",
		"moduleResolution": "Bundler",
		"jsx": "react-jsx",
		"noEmit": true,
		"verbatimModuleSyntax": true,
		"useDefineForClassFields": true,
		"resolveJsonModule": true,
		"skipLibCheck": true,
		"paths": { "@/*": ["./src/*"] }
	},
	"include": ["src"]
}
